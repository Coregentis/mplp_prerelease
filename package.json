{
    "name": "mplp-protocol-oss",
    "version": "1.0.0",
    "description": "MPLP Protocol v1.0 - Multi-Agent Lifecycle Protocol (OSS Package)",
    "repository": {
        "type": "git",
        "url": "https://github.com/Coregentis/MPLP-Protocol.git"
    },
    "author": "Coregentis",
    "license": "Apache-2.0",
    "private": true,
    "scripts": {
        "docs:dev": "cd docs && npm run start",
        "docs:build": "cd docs && npm run build",
        "package:surface:check": "node scripts/semantic/package-preflight-harness.mjs --mode=surface --no-publish",
        "package:preflight:research": "node scripts/semantic/package-preflight-harness.mjs --mode=research --no-publish",
        "package:preflight:verify-plan": "node scripts/semantic/package-preflight-harness.mjs --mode=verify-plan --no-publish",
        "test": "echo 'No root tests configured'",
        "lint": "echo 'No root linting configured'"
    },
    "devDependencies": {
        "js-yaml": "^4.1.0",
        "minimatch": "^10.0.1"
    }
}
